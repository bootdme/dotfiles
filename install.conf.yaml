- defaults:
    link:
      create: true
      relink: true

# Remove dead symlinks
- clean:
    ~/:
    ~/.config:
      recursive: true

- shell:
    - [git submodule update --init --recursive, Installing submodules]

- link:
    # Neovim
    ~/.config/nvim:
      glob: true
      path: nvim/*
    # Fish
    ~/.config/fish:
      glob: true
      path: fish/*
    # Kitty
    ~/.config/kitty: kitty/
    # Git
    ~/.config/git/config.base: git/config.base
- ifmacos:
    - link:
        # Nu Shell
        ~/Library/Application Support/nushell/env.nu: nushell/env.nu
        ~/Library/Application Support/nushell/completion.nu: nushell/completion.nu
        ~/Library/Application Support/nushell/config.nu: nushell/config.nu
        ~/Library/Application Support/nushell/aliases.nu: nushell/aliases.nu
    - tap: [homebrew/services, rsteube/homebrew-tap]
    - brew: [bitwarden, brave-browser, cmake, discord, font-hack-nerd-font, fish, go, gnupg, kitty, mullvad-browser, neovim, pinentry-mac, postgresql@16, postman, rsteube/tap/carapace, spotify, tor-browser, wget]

- ifanylinux:
    - link:
        # Nu Shell
        ~/.config/nushell/env.nu: nushell/env.nu
        ~/.config/nushell/config.nu: nushell/config.nu
        ~/.config/nushell/aliases.nu: nushell/aliases.nu

- omnipkg:
    - install:
        [
          { dnf: cmake },
          { dnf: golang },
          { dnf: kitty },
          { dnf: neovim },
          { dnf: openssl-devel },
          { dnf: postgresql },
        ]

- cargo:
    - bat
    - exa
    - fnm
    - nu
    - zoxide
